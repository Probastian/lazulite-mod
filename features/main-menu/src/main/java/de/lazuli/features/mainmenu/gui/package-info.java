/**
 * Permanent, empty placeholder -- this feature's own widget/rendering code
 * lives in each platform module's Version Adapter layer
 * ({@code MainMenuScreen}, {@code MainMenuBackgroundRenderer}, per-panel
 * renderers), not here; this package stays empty by design.
 */
package de.lazuli.features.mainmenu.gui;
