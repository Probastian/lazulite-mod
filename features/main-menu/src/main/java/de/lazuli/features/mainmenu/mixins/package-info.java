/**
 * Permanent, empty placeholder -- this feature needs no mixins in v1 (screen
 * replacement is achieved via the Fabric-API-only
 * {@code ClientLifecycleEvents.CLIENT_STARTED}/{@code setScreen} mechanism,
 * spec Architecture); this package stays empty by design.
 */
package de.lazuli.features.mainmenu.mixins;
