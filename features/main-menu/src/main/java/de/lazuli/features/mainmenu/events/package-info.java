/**
 * Permanent, empty placeholder -- this feature has no published event/callback
 * types in v1 (spec Events; state transitions are plain method calls from
 * {@code MainMenuScreen}'s input handlers, not events in the pub/sub sense).
 */
package de.lazuli.features.mainmenu.events;
