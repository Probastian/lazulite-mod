/**
 * Placeholder package: this feature needs no generic event-bus glue (this
 * repo has no event bus at all) -- reserved for structural parity with other
 * feature modules' own {@code events} package, per {@code steam-world-hosting}'s
 * identical placeholder convention.
 */
package de.lazuli.features.richpresence.events;
