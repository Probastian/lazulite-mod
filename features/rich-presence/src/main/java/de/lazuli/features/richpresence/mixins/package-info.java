/**
 * Placeholder package: every signal this feature reads is gathered via
 * ordinary client-tick polling of already-public state -- no mixin needed,
 * a materially simpler feature than {@code steam-world-hosting} in this
 * respect (that feature's plan, "Existing Implementation"). Reserved for
 * structural parity with other feature modules' own {@code mixins} package.
 */
package de.lazuli.features.richpresence.mixins;
