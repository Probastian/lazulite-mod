/**
 * Placeholder package -- this feature defines no new cross-feature event bus
 * type (spec Events); consumes only Fabric API's own
 * {@code ScreenEvents.AFTER_INIT}/{@code ClientTickEvents.END_CLIENT_TICK}
 * and steamworks4j's {@code SteamFriendsCallback}, both confined to
 * {@code platform/}/{@code services/FriendsService} respectively.
 */
package de.lazuli.features.friendssidebar.events;
