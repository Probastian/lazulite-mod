/**
 * Permanent empty placeholder (NFR4, {@code feature-guidelines.md}) -- any
 * real {@code @Mixin} this feature ever needs lives in
 * {@code platform/fabric-<version>/.../mixin/} instead, never here.
 */
package de.lazuli.features.friendssidebar.mixins;
