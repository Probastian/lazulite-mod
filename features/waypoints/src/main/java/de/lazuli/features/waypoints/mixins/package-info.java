/**
 * Placeholder package: all mixin code (the compass-bar HUD injection) lives
 * platform-side ({@code platform/fabric-*}'s own {@code mixin} package), never in this
 * plain-Java feature module -- this feature module owns no mixin code of its
 * own (spec Architecture, Dependency Rules table).
 */
package de.lazuli.features.waypoints.mixins;
