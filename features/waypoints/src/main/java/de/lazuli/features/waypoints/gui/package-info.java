/**
 * Placeholder package: the Waypoint Manager panel UI and compass-bar HUD
 * rendering live platform-side ({@code WaypointManagerPanel}/the compass-bar
 * mixin, per platform module), matching {@code features/tweaks}'s
 * {@code TweaksPanel} precedent -- this feature module owns no GUI code of
 * its own (spec Architecture).
 */
package de.lazuli.features.waypoints.gui;
