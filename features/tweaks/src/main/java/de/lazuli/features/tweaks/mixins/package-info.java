/**
 * Placeholder package: this feature module is deliberately plain-Java with
 * no Minecraft classpath (Architecture Decision 2 in the implementation
 * plan) -- every real mixin/render-hook injection lives per-platform-module,
 * against that platform's own Minecraft classpath, not here.
 */
package de.lazuli.features.tweaks.mixins;
