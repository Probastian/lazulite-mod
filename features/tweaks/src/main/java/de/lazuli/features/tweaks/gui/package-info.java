/**
 * Placeholder package: the Tweaks tab UI lives platform-side
 * ({@code TweaksPanel} per platform module), matching the existing main-menu
 * tab precedent (Achievements/Statistics panels are platform-side too) --
 * this feature module owns no GUI code of its own.
 */
package de.lazuli.features.tweaks.gui;
