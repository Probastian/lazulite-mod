/**
 * Permanent, empty placeholder -- this feature needs zero {@code @Mixin}
 * files (spec Decision 3): {@code ServerBrowserListWidget} is a genuinely
 * new subclass of the vanilla list base class, not an insertion of synthetic
 * rows into a foreign, already-constructed vanilla list, so the
 * {@code addEntry}/{@code clearEntries} protected-access problem
 * {@code steam-cloud-sync}'s Group 6 hit never arises here.
 */
package de.lazuli.features.serverbrowser.mixins;
