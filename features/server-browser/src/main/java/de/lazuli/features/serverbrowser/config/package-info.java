/**
 * Permanent, empty placeholder -- this feature has no persisted config file
 * in v1 (spec Configuration; sort/filter/source are session-only, FR3.7).
 * Unlike other features' {@code config/} packages, this is not "temporarily
 * empty pending a future config file"; it stays empty by design.
 */
package de.lazuli.features.serverbrowser.config;
