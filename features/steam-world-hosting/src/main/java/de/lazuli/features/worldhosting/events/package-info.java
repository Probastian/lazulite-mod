/**
 * Placeholder package. State changes this feature cares about (world
 * load/unload) are observed at the platform composition-root level via Fabric's
 * own lifecycle/tick events, not a custom event bus (spec Events). No shareable
 * common-Java event code lives here.
 */
package de.lazuli.features.worldhosting.events;
