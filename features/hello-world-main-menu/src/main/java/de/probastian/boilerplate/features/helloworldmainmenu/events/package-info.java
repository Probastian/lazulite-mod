/**
 * Placeholder package, deliberately empty for v1.
 *
 * <p>This feature defines no new cross-feature event on a shared event bus:
 * no such bus exists yet in {@code services/}, and no other feature
 * currently exists to react to this one (see the feature's
 * {@code specification.md}, "Events" section). The package is kept
 * git-tracked (via this file) so the required {@code events/} folder from
 * {@code feature-guidelines.md} exists and is discoverable, ready for future
 * event types if this feature ever needs to publish one.
 */
package de.probastian.boilerplate.features.helloworldmainmenu.events;
