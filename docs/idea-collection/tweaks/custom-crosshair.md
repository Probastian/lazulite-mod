# Tweak: Custom Crosshair

If on, a previously configured custom crosshair will replace the vanilla crosshair.

# Configurables

- Various shape modifiers, similar to Counter-Strike 2 crosshair configurator.
- Color: Either RGB (with RGB sliders) or vanilla (color-inverted)