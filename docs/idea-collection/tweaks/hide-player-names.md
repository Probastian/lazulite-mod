# Tweak: Hide player names

If on it will hide player names, usually located floating above player heads.

# Configurables:

- Global, Range (inclusive) or Range (exclusive): Global hides all, range-inclusive hides all inside configured range, range-exlusive hides all outside configured range