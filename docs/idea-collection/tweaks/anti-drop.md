# Tweak: Anti-Drop

If on, prevents certain items from being accidentally dropped. If the player tries to drop the item the tweak prevents the actual drop and displays a message that dropping was prevented by this tweak.

# Configurables

- White-list
- Shift-Q to force drop
- Hot-Key to quick white-list toggle a held or hovered item