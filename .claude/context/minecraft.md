# Minecraft Development Guide

## Documentation Priority
1. Official Fabric documentation
2. Fabric API source
3. Fabric API JavaDocs
4. Mojang mappings
5. Parchment
6. Sponge Mixin Wiki
7. Minecraft source
8. Minecraft Wiki
9. Community articles

## Mappings
Prefer Mojang mappings where possible.
Use Parchment for parameter names and documentation.
Avoid exposing Yarn/intermediary names in public APIs.

## Research Rules
Always verify official documentation before implementing:
- Rendering
- Mixins
- Registries
- Networking
- Commands
- Data generation
- World generation
- Entity AI

Never invent APIs.

## Wrappers
Wrap verbose Minecraft APIs.

Instead of:
Registry.register(...)

Prefer:
Registrar.item(...)
PlatformRegistry.registerItem(...)

## Version Compatibility
Business logic belongs in common modules.
Minecraft implementation belongs in platform modules.
No version checks inside features.

## Known Cross-Version API Differences
A living record of concrete API divergences discovered between the supported
Minecraft/Fabric versions, so later features don't re-research something
already found. Append a row whenever implementation work turns up a real
divergence (not just package renames already covered by the Obfuscation
Boundary table below) — include which versions it affects.

| API / Concept | ≤1.21.11 (Yarn) | ≥26.1 (Mojang) | Found while |
|---|---|---|---|
| Adding a widget to an existing screen's widget list | `net.fabricmc.fabric.api.client.screen.v1.Screens.getButtons(Screen)` | `Screens.getWidgets(Screen)` — renamed in fabric-api, not just remapped | `hello-world-main-menu` (FabricMainMenuHook) |
| Toast queue accessor (post a `SystemToast`) — **not just a mapping rename; the owning object moved**, confirmed via `javap -p` against each module's own resolved Minecraft jar | `MinecraftClient.getInstance().getToastManager()`, `net.minecraft.client.toast.ToastManager`/`SystemToast`, `SystemToast.add(ToastManager, SystemToast.Type, Text, Text)`, `SystemToast.Type.PERIODIC_NOTIFICATION` | **26.1**: `Minecraft.getInstance().getToastManager()` (still directly on `Minecraft`, same shape as 1.21.11, just Mojmap-named). **26.2**: the accessor *moved off* `Minecraft` onto `Gui` — `Minecraft.getInstance().gui.toastManager()`. Both 26.x versions otherwise share `net.minecraft.client.gui.components.toasts.ToastManager`/`SystemToast`, `SystemToast.add(ToastManager, SystemToast.SystemToastId, Component, Component)`, `SystemToast.SystemToastId.PERIODIC_NOTIFICATION`. | `friends-sidebar` (FabricToastService, specification-invite-to-game.md Phase 0) |
| Non-interactive text widget | `net.minecraft.client.gui.widget.TextWidget` | `net.minecraft.client.gui.components.StringWidget` | `hello-world-main-menu` (FabricMainMenuHook) |
| Generic-model 3D "picture-in-picture" GUI render call (arbitrary hand-authored `ModelPart`/`ModelData` hierarchy with its own free camera, drawn into a screen-space rectangle) — confirmed via `javap` against each module's own resolved Minecraft jar, **not just a rename: real capability divergence, and 26.2 is the outlier, not 1.21.11** | `DrawContext.addPlayerSkin(PlayerEntityModel, Identifier, float,float,float,float, int,int,int,int)` — restricted to a real biped `PlayerEntityModel` only (internally resolves named parts `head`/`hat`/`body`/`left_arm`/`right_arm`/`left_leg`/`right_leg` + `left_sleeve`/`right_sleeve`/`left_pants`/`right_pants`/`jacket` via `ModelPart.getChild(String)`, throws if any are missing) — no overload accepts an arbitrary `Model`/`Model.SinglePartModel` with a free camera and an arbitrary `Identifier` texture (the only other generic-model overload, `addSign(Model.SinglePartModel, float, WoodType, ...)`, hard-binds its texture to a `WoodType`, not an arbitrary `Identifier`) | **26.1**: identical restriction to 1.21.11 — `GuiGraphicsExtractor.skin(PlayerModel, Identifier, ...)` only, same named-part requirements, same `sign(Model.Simple, float, WoodType, ...)` WoodType-bound-texture-only fallback. **26.2**: `GuiGraphicsExtractor.skin(Model.Simple, Identifier, ...)` — a **generic** overload accepting any hand-authored `ModelPart` hierarchy with an arbitrary texture, confirmed present only on 26.2 among all three supported versions. A from-scratch non-biped 3D scene (e.g. this repo's own sky/mountains/ground background) can only be submitted through this mechanism on 26.2; on 26.1/1.21.11 only a real player-shaped model can use it (workaround used by `main-menu`: reshape the model's named parts to satisfy `PlayerModel`/`PlayerEntityModel`'s requirements for anything biped-shaped, degrade non-biped scene geometry to 2D `fill`/`fillGradient` screen-space bands instead) | `main-menu` (`MainMenuBackgroundRenderer`, Sequencing step 13 porting) |
| **Follow-up to the row above, javap/bytecode-verified (`unified-mainmenu-background`)**: does `PlayerEntityModel`/`PlayerModel`'s required-child constructor check (`getChild(String)`, throws if missing) also restrict what the model *renders*, or only what it *requires to exist*? Confirmed via full bytecode trace (`Model`/`EntityModel`'s `final render(...)` methods delegate to `ModelPart.render(...)` on the model's own root, which iterates the entire backing `children` `Map` with **zero name filtering**) that extra sibling/child bones attached anywhere in the same hierarchy render exactly like 26.2's generic `Model.Simple` path — the required-name check only gates construction, not rendering. **Caveat**: extra bones are not auto-*posed* by `BipedEntityModel`/`HumanoidModel`'s `setAngles`/`setupAnim` — only rendered; per-frame animation of a non-biped bone still needs a manual `getChild(name)` + field-mutate, same as character posing already does. This let `main-menu` merge 26.1/1.21.11's previously-2D scenery into the same single `skin(PlayerModel,...)`/`addPlayerSkin(PlayerEntityModel,...)` call as the character (one shared `:common`-sourced `MeshDefinition`/`ModelData` per platform, scenery bones as extra top-level children) instead of a separate `fill`/`fillGradient` pass. **In-game-confirmed: no** (bytecode-verified only as of this feature's implementation pass — a real launch/visual check is still outstanding, see `docs/specs/unified-mainmenu-background-plan.md` Risk 6). Related, separate finding also from this feature: 1.21.11's builder-API class family (`ModelData`/`ModelPartData`/`ModelPartBuilder`/`ModelTransform`/`Dilation`/`TexturedModelData`) and 26.1/26.2's (`MeshDefinition`/`PartDefinition`/`CubeListBuilder`/`PartPose`/`CubeDeformation`/`LayerDefinition`) share no common supertype/method shape — no single Java source file can build the mesh for all three platforms using the platforms' own Minecraft-provided builder types, so only the plain-data cube geometry (name/pivot/box/UV, `common/.../mainmenu/MeshCubeSpec`+`MainMenuMeshDefinitions`) is shared; each platform's `MainMenuBackgroundRenderer` still hand-translates that data into its own native builder calls. Also: 26.2's character bones were renamed from this feature's pre-existing `torso`/`hair` naming to `body`/`hat`, matching 26.1/1.21.11's naming (both already constrained by `PlayerModel`/`PlayerEntityModel`'s required names) — purely internal, not user-visible. | 26.1 and 26.2 share an identical builder-API class family (`MeshDefinition`/`PartDefinition`/`CubeListBuilder`/`PartPose`, under `net.minecraft.client.model.geom*`); 1.21.11 uses the separate `ModelData`/`ModelPartData`/`ModelPartBuilder`/`ModelTransform` family under `net.minecraft.client.model` (Yarn) — no shared source possible across the two families | Same | `main-menu` (`unified-mainmenu-background`, implementation) |
| Text/component type | `net.minecraft.text.Text`, `Text.literal(String)` | `net.minecraft.network.chat.Component`, `Component.literal(String)` | `hello-world-main-menu` (FabricMainMenuHook) |
| Per-frame lightmap/gamma computation — **not just a rename: 26.x moved to the render-state-extraction model, the spec/plan's guessed `LightTexture` class does not exist at all**, confirmed via `javap -p` | `net.minecraft.client.render.LightmapTextureManager.update(float)` reads gamma via `GameOptions.getGamma().getValue()` inline and rebuilds the lightmap texture directly | `net.minecraft.client.renderer.LightmapRenderStateExtractor.extract(LightmapRenderState, float)` writes the gamma-derived value into the public mutable `LightmapRenderState.brightness` field (confirmed identical on 26.1/26.2); there is no `LightTexture` class on 26.x | `tweaks-hooks-wiring` (T2 Force Brightness implementation) |
| Client-side "drop selected item" entry point | `net.minecraft.client.network.ClientPlayerEntity.dropSelectedItem(boolean)` (also mirrored on `PlayerInventory.dropSelectedItem(boolean)`) | `net.minecraft.client.player.LocalPlayer.drop(boolean)` (confirmed identical 26.1/26.2); the plan's guessed `MultiPlayerGameMode`/`ClientPlayerInteractionManager` have no drop method on either side | `tweaks-hooks-wiring` (T1 Anti-Drop implementation) |
| Nametag render gate | `EntityRenderer.hasLabel(T, double)` (boolean gate consulted by `renderLabelIfPresent`, itself a render-command-queue method, not an immediate draw) | `EntityRenderer.shouldShowName(T, double)` (confirmed identical 26.1/26.2); neither side has a method literally named `renderNameTag`/`renderLabelIfPresent`-as-a-cancellable-draw | `tweaks-hooks-wiring` (T8 Hide Player Names implementation) |
| Animated-sprite per-frame tick — **structural divergence, not a rename**: 1.21.11 is single-class, 26.x splits definition from playback state | `SpriteContents$Animator.tick()` — single inner class, no back-reference field to its owning `SpriteContents`/`Identifier` | `SpriteContents$AnimatedTexture` (immutable per-sprite definition, non-static inner class with a `this$0` back-ref) produces a `SpriteContents$AnimationState` via `createAnimationState(...)`; `AnimationState.tick()` (not `AnimatedTexture`) is the real per-frame advance, and it also has no direct back-reference to the owning sprite id — confirmed identical 26.1/26.2 | `tweaks-hooks-wiring` (T6 Disable Animations implementation) |
| Crosshair/underwater-overlay/boss-bar HUD render — **26.1 and 26.2 are NOT identical here**, contradicting the general "26.1/26.2 usually match" assumption | `InGameHud.renderCrosshair(DrawContext, RenderTickCounter)` (direct cancellable draw); `InGameHud.renderOverlay(DrawContext, Identifier, float)` is shared by water/portal/spyglass/nausea/powder-snow overlays (must discriminate by the `Identifier` arg) | 26.1: `Gui.extractCrosshair(GuiGraphicsExtractor, DeltaTracker)` / `Gui.extractTextureOverlay(GuiGraphicsExtractor, Identifier, float)`. 26.2: the **same-named, same-signature** methods moved off `Gui` onto a new class `Hud` (`Gui` now just holds `public final Hud hud`) — `Gui.extractCrosshair`/`extractTextureOverlay` do not exist on 26.2. `BossHealthOverlay` itself (with `extractRenderState(GuiGraphicsExtractor)`/private `extractBar(...)`) is unaffected by the split and is identical on 26.1/26.2 | `tweaks-hooks-wiring` (T5 Custom Crosshair, T9 Clear Water, T12 Disable Boss Bars implementation) |
| Launching a world from just a save-folder id string (no pre-existing list-row widget in hand) — **real capability gap, not a rename**, confirmed via `javap` against each module's own resolved Minecraft jar | 1.21.11 has **no** `createWorldOpenFlows()`-equivalent anywhere (`MinecraftClient` has no such method; no class under `net.minecraft.client.gui.screen.world` is named `WorldOpenFlows`/similar) — the only real-Play entry point is `WorldListWidget.WorldEntry#play()`, a **public but non-static inner class** instance method requiring a real `LevelSummary` (constructible synchronously via `LevelStorage.Session.readLevelProperties()` + the also-public `Session.getLevelSummary(Dynamic<?>)`, then `outerWidget.new WorldEntry(outerWidget, summary)` — qualified-new syntax required since it's a non-static inner class) | 26.1/26.2: `Minecraft.getInstance().createWorldOpenFlows().openWorld(String levelId, Runnable)` — a real public API taking just the save-folder id, no widget/entry construction needed at all (already used by `HomePanel`'s Recent-section Play action on both) | `cloud-world-entry-parity` (Requirement 4/5, "Download & Play" world-launch implementation) |
| Pause-menu screen class name (confirmed via `javap` against each module's own resolved jar) — package-rename-shaped, but load-bearing since it gates an `instanceof` mixin branch, not just a mapping detail | `net.minecraft.client.gui.screen.GameMenuScreen` — never named `PauseScreen` on Yarn; constructed by `MinecraftClient`'s own Esc-handler and handed straight to `MinecraftClient.setScreen(Screen)`, the same choke point the existing title-screen redirect mixin already targets | `net.minecraft.client.gui.screens.PauseScreen` (confirmed identical 26.1/26.2); constructed by `Gui.setPauseScreen(boolean, boolean)` and handed straight to `Gui.setScreen(Screen)`, same choke point as the title-screen redirect | `main-menu-pause-integration` (pause-trigger mixin) |
| **Correction/follow-up to the row above — the "26.1/26.2 both go through `Gui.setScreen`" claim in that row is WRONG for 26.1**, confirmed via `javap -p`/`javap -c` against 26.1's own resolved jar (root-caused a startup `InvalidInjectionException` from a mixin blindly copied from the 26.2 module without re-verifying 26.1's own shape) — this is a real structural divergence between 26.1 and 26.2, not just a package rename, despite the two usually matching | N/A (Yarn 1.21.11 already covered by the row above; not re-verified again here) | **26.1**: `net.minecraft.client.gui.Gui` has **no screen-related methods at all** (`javap -p` on `Gui.class` only surfaces debug-overlay methods) — `setScreen(Screen)`/`setScreenAndShow(Screen)` and the pause-trigger (`pauseGame(boolean)`, constructs `new PauseScreen(boolean)`) all live directly on `Minecraft` itself; `setScreenAndShow` simply delegates to `this.setScreen(screen)` (confirmed via `javap -c`), and the null-argument "auto-recover to a raw `TitleScreen`" branch also lives inside `Minecraft.setScreen` itself (`this.level == null` check). A mixin must target `@Mixin(Minecraft.class)` / method `"setScreen"` on 26.1. **26.2**: as the row above states, the equivalent logic really is on `Gui.setScreen`/`Gui.setPauseScreen` — confirm the target class per-version via `javap -p`, never assume 26.1 and 26.2 share this choke point without re-checking | `fabric-26.1` startup crash bugfix (`GuiTitleScreenRedirectMixin` re-targeted from `Gui` to `Minecraft`) |
| Integrated server's "save everything" method — **not just a rename: whether the method is overridden on the integrated-server subclass itself differs**, confirmed via `javap` against each module's own resolved jar; load-bearing for any `@Mixin` targeting it, since the mixin's target class must be the class that actually declares/overrides the method | `net.minecraft.server.integrated.IntegratedServer#saveAll(boolean, boolean, boolean)Z` — **overridden directly on `IntegratedServer` itself** (Yarn's mapped name for vanilla's `saveEverything`); a `@Mixin(IntegratedServer.class)` injecting into `saveAll` works | `net.minecraft.server.MinecraftServer#saveEverything(boolean, boolean, boolean)Z` (confirmed identical 26.1/26.2) — **declared only on `MinecraftServer`, NOT overridden on `net.minecraft.client.server.IntegratedServer`**; a mixin must target `MinecraftServer` itself (guarded with `instanceof IntegratedServer` if it should only fire for the integrated/singleplayer server) rather than `IntegratedServer`, since Sponge Mixin's `@Inject` requires the method to be declared/overridden in the exact target class | `cloud-sync-status-ui` (`WorldSaveHookMixin`, FR-T.1 on-save upload trigger) |
| Driving `WorldListWidget`'s async save-folder scan **headlessly** (built via `WorldListWidget.Builder`/`toWidget()` but never added to a screen's real widget tree, never rendered by vanilla) — **real capability gap, not a rename**, confirmed via `javap -c` against 1.21.11's own resolved Minecraft jar (decompiled bytecode of `load()`/`tryGet()`/`show(List)`/`renderWidget(...)`) | `WorldListWidget#load()` only stores a `CompletableFuture<List<LevelSummary>>` into a private `levelsFuture` field. The private `tryGet()` (drains the future via `getNow(null)`) and private `show(List)` (the only method that actually calls `addEntry(...)`, i.e. the only thing that populates `children()`) are **each called from exactly one place: `WorldListWidget#renderWidget(DrawContext, int, int, float)`** — there is no other path, public or private, that transfers a completed future's results into `children()`. A `WorldListWidget` instance that is never added to a screen (so its own `renderWidget` is never invoked by the vanilla widget-list render loop) will have `children()` stay permanently empty no matter how long anything polls it, even though the underlying `CompletableFuture` itself completes normally in the background. Fix: don't rely on `WorldListWidget`'s own `children()`/`show()` pipeline for a headless instance at all — drive `LevelStorage.getLevelList()`/`LevelStorage.loadSummaries(LevelList)` directly (the same real future `loadLevels()` wraps internally) and build `WorldListWidget.WorldEntry` rows manually via the already-public 2-arg inner-class constructor (`outerWidget.new WorldEntry(outerWidget, summary)`) once that future completes | Not applicable — 26.1/26.2 never used `WorldListWidget` for this at all, calling `LevelStorageSource.findLevelCandidates()`/`loadLevelSummaries(...)` directly (see the "Launching a world from just a save-folder id string" row above for the related `WorldEntry#play()`-vs-`createWorldOpenFlows()` gap) | `cloud-world-entry-parity` live-test fixes (Bug A: 1.21.11 Worlds tab showing 0 local saved worlds while cloud-only worlds still rendered correctly) |
| Blurred/darkened frozen-world screen background (vanilla `PauseScreen`/`GameMenuScreen`'s own background, not a solid fill) — **directly invokable from any `Screen` subclass, not gated by any `instanceof` check on the current screen, but ALREADY auto-invoked once per frame by the base class's own final draw-entry-point wrapper method on every version — do NOT also call it manually from your own `render`/`extractRenderState` override, or it double-fires and crashes** (confirmed via `javap` against each module's own resolved jar; the double-call crash — `GuiRenderState.blurBeforeThisStratum()` throwing "Can only blur once per frame" — was reproduced live in-game on 26.2 during `main-menu-pause-integration` before this row was corrected) | `Screen.renderBackground(DrawContext, int, int, float)` — public, inherited; when `MinecraftClient.world != null` it skips the panorama and calls `Screen.applyBlur(DrawContext)` (blur amount driven by `GameOptions`) + `Screen.renderDarkening(DrawContext)`. Already called automatically, once per frame, by the `final` `Screen.renderWithTooltip(DrawContext, int, int, float)` (bytecode offset 10) *before* it invokes the virtual `render(...)` override (offset 23) — a custom `Screen` subclass's own `render(...)` must NOT call `renderBackground(...)` again itself | `Screen.extractBackground(GuiGraphicsExtractor, int, int, float)` — public, inherited (confirmed identical 26.1/26.2); when `Minecraft.level != null` it skips the panorama and calls `Screen.extractBlurredBackground(GuiGraphicsExtractor)` (blur amount driven by `Options.getMenuBackgroundBlurriness()`, applies via `GuiGraphicsExtractor.blurBeforeThisStratum()`) + `Screen.extractMenuBackground(GuiGraphicsExtractor)`. Already called automatically, once per frame, by the `final` `Screen.extractRenderStateWithTooltipAndSubtitles(GuiGraphicsExtractor, int, int, float)` *before* it invokes the virtual `extractRenderState(...)` override — same "do not call again yourself" rule as 1.21.11 | `main-menu-pause-integration` (pause-context background: resolved the plan's "preferred vs. fallback" spike in favor of "preferred" — a subclass never needs to call this itself at all, since the base class's own draw-entry wrapper already invokes it before every `render`/`extractRenderState` override on all three platforms; no mixin needed) |
| Chat message add entry point / signature-data shape | `ChatHud.addMessage(Text)` and `addMessage(Text, MessageSignatureData, MessageIndicator)` (both public); `MessageSignatureData` is just raw signature bytes (`byte[] data()`), **carries no sender UUID/profile** — resolving a chat line's sender for anything beyond the signature bytes needs an earlier network-layer hook, not `ChatHud` | `ChatComponent.addMessage(Component, MessageSignature, GuiMessageSource, GuiMessageTag)` is **private** (not the guessed public single-`Component` overload) — confirmed identical 26.1/26.2 | `tweaks-hooks-wiring` (T3 Chat Filter implementation; T4 Chat Player Heads sender-resolution investigation) |
| Hotkey registration Fabric API submodule + `KeyBinding`/`KeyMapping` category type — confirmed via `javap` against each module's own resolved jar/POM, **not just a rename: the artifact/coordinate itself differs, and a naive "category is a plain `String`" assumption (common in older Minecraft-modding docs/tutorials) is wrong on every one of this repo's three supported versions** | Artifact `net.fabricmc.fabric-api:fabric-key-binding-api-v1` (this exact version pinned: `1.1.7+4fc5413f3e`), class `net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper.registerKeyBinding(KeyBinding): KeyBinding`; `net.minecraft.client.option.KeyBinding`'s category parameter is `KeyBinding.Category` (a record, not `String`) — construct/reuse via `KeyBinding.Category.create(Identifier)`, id via `net.minecraft.util.Identifier.of(namespace, path)`; edge-triggered poll is `KeyBinding.wasPressed()`, rebind via `KeyBinding.setBoundKey(InputUtil.Key)` (capture from a key event via `InputUtil.fromKeyCode(KeyInput)`) + `KeyBinding.updateKeysByCode()` | Artifact `net.fabricmc.fabric-api:fabric-key-mapping-api-v1` (**renamed** from `fabric-key-binding-api-v1`; this repo's fabric-api POM for both 26.1 (`0.145.1+26.1`) and 26.2 (`0.154.2+26.2`) does not even declare `fabric-key-binding-api-v1` as a dependency — pin the replacement directly, versions confirmed: 26.1 `2.0.4+e2bdee7847`, 26.2 `2.0.5+e2bdee789e`; `fabricApi.module("fabric-key-mapping-api-v1", fabric_api_version)` has **no Loom module-registry entry** for either of this repo's pinned `fabric_api_version`s, so the coordinate must be pinned as a literal `implementation` string, not resolved via the `fabricApi.module(...)` helper), class `net.fabricmc.fabric.api.client.keymapping.v1.KeyMappingHelper.registerKeyMapping(KeyMapping): KeyMapping`; `net.minecraft.client.KeyMapping`'s category parameter is `KeyMapping.Category` (a record, not `String`) — construct/reuse via `KeyMapping.Category.register(Identifier)`, id via `net.minecraft.resources.Identifier.fromNamespaceAndPath(namespace, path)`; edge-triggered poll is `KeyMapping.consumeClick()` (**not** `wasPressed()` -- that method does not exist on this version's `KeyMapping`), rebind via `KeyMapping.setKey(InputConstants.Key)` (capture from a key event via `InputConstants.getKey(KeyEvent)`) + the static `KeyMapping.resetMapping()` | `tweaks` (framework implementation, `TweaksKeyBindings`) |
| Client singleton | `net.minecraft.client.MinecraftClient` | `net.minecraft.client.Minecraft` | `hello-world-main-menu` (FabricMainMenuHook) |
| `setScreen(null)`'s internal "return to default screen" auto-recovery — **not a rename, a real behavioral trap for any `@ModifyVariable`-based `setScreen` redirect mixin, confirmed identical in shape on all three platforms via `javap -c`** | `MinecraftClient.setScreen(Screen)`: when the `screen` argument is `null` AND `this.world == null`, the method body constructs a raw `new TitleScreen()` *internally* (reassigning its own local variable, not the incoming argument) before installing it — this reassignment happens entirely inside the method body, past the point any `@ModifyVariable(at = @At("HEAD"), argsOnly = true)` mixin already ran, so it is invisible to that mixin; a `null`-arg caller silently bypasses any `instanceof TitleScreen`-based redirect and gets vanilla's real `TitleScreen`, not a redirected replacement | `Gui.setScreen(Screen)`: identical shape — `screen == null && this.minecraft.level == null` internally constructs `new TitleScreen()` past the `@ModifyVariable` HEAD injection point (confirmed identical 26.1/26.2) | `main-menu-pause-integration` (quit-to-title bugfix follow-up: any `setScreen`/`setScreen`-redirect mixin pattern in this repo must add an explicit `screen == null && <no level/world loaded>` arm alongside its `instanceof` checks, replicating vanilla's own condition, or it will miss this path) |
| Saved/pinged server's MOTD field name — **not an obvious rename, confirmed via `javap` against each module's own resolved jar** | `net.minecraft.client.network.ServerInfo.label` (a `Text`) is the MOTD; `ServerInfo.playerCountLabel` is the separate status/player-count line. `net.minecraft.client.network.MultiplayerServerListPinger.add(ServerInfo, Runnable onSuccess, Runnable onFail, NetworkingBackend)` pings it (`NetworkingBackend.remote(boolean useNativeTransport)`); ping/player/motd mutate the same `ServerInfo` instance in place, both callbacks are just refresh hooks. World icon: `net.minecraft.world.level.storage.LevelSummary.getIconPath()`. World/server favicon texture+fallback: `net.minecraft.client.gui.screen.world.WorldIcon.forWorld(TextureManager, String)`/`.forServer(...)`, `.load(NativeImage)`, `.getTextureId()` — already points at a built-in "missing" sprite before `.load()` is ever called. | `net.minecraft.client.multiplayer.ServerData.motd` (a `Component`) is the MOTD; `ServerData.status` is the separate status line — same two-field split, different field name for the MOTD one specifically. `net.minecraft.client.multiplayer.ServerStatusPinger.pingServer(ServerData, Runnable, Runnable, EventLoopGroupHolder)` pings it (`EventLoopGroupHolder.remote(boolean useNativeTransport)`), same in-place-mutation/callback shape. World icon: `net.minecraft.world.level.storage.LevelSummary.getIcon()` returns the `Path` directly (no separate accessor name change, just confirmed same shape). World/server favicon texture+fallback: `net.minecraft.client.gui.screens.FaviconTexture.forWorld(TextureManager, String)`/`.forServer(...)`, `.upload(NativeImage)`, `.textureLocation()` — identical shape/behavior to `WorldIcon` on both 26.1 and 26.2. | `main-menu` (post-launch-fixes FX4.1/FX4.2/FX4.4/FX4.5, `IconTextureCache`/`ServersPanel`) |
| Singleplayer world-select screen's internal scrolling list widget (nested inside `SelectWorldScreen`, holds one row per save) — **class name confirmed via `javap -p` against this repo's actual resolved Minecraft jars** (`.gradle/loom-cache/minecraftMaven/net/minecraft/minecraft-merged*/**`), superseding the planning-time web-research-only guess | `net.minecraft.client.gui.screen.world.WorldListWidget`, extends `net.minecraft.client.gui.widget.AlwaysSelectedEntryListWidget<Entry>` extends `net.minecraft.client.gui.widget.EntryListWidget<E>`. `EntryListWidget.addEntry(E)`/`clearEntries()` are `protected`; `children()` is `public final` and returns the live backing list directly (no defensive copy in the accessor itself). `WorldListWidget.Entry` is `abstract` with a **public** no-arg constructor and a non-abstract `getLevel()` (safe to subclass directly, no constructor-accessibility mixin needed). `SelectWorldScreen.levelList` is a `private` field (must locate the widget instance via the Screen's own `children()`/`Screens.getWidgets` list, filtered by `instanceof`, never a direct field reference). | `net.minecraft.client.gui.screens.worldselection.WorldSelectionList`, extends `net.minecraft.client.gui.components.ObjectSelectionList<Entry>` extends `net.minecraft.client.gui.components.AbstractSelectionList<E>` — **not** `EntryListWidget`; that Yarn-era base-class name does not exist at all under Mojang's official mapping used by 26.1/26.2 (a genuine naming divergence beyond a simple rename, confirmed absent via `javap`/jar-entry listing). `AbstractSelectionList.addEntry(E)`/`clearEntries()` are `protected` (same visibility as the Yarn side); `WorldSelectionList` itself additionally overrides `clearEntries()` (still `protected`). `children()` is `public final`, same live-list behavior as the Yarn side. `WorldSelectionList.Entry` is `abstract` with a **public** no-arg constructor and non-abstract `getLevelSummary()` — same subclass-directly approach works, no constructor mixin needed. `SelectWorldScreen.list` is a `private` field, identical constraint to the Yarn side. Confirmed identical across both `26.2` and `26.1` jars (same class/method set). | `steam-cloud-sync` (implementation, mandatory-first-step `javap` pass) |
| **26.x's `Screen` rendering entry point is a render-state *extraction* model, not immediate-mode `PoseStack`/`VertexConsumer` draw calls** — a `Screen` no longer issues raw 3D draw calls itself; `Screen.extractRenderState(GuiGraphicsExtractor, int, int, float)` only records descriptor objects (`GuiElementRenderState`s) into a `GuiRenderState`, rendered by a separate later pass. There is **no public entry point to submit arbitrary hand-rolled 3D vertex geometry with a fully custom camera** the way `Screen.render(PoseStack,...)` used to allow. The closest usable primitive for a real, camera-lit 3D scene composited into an arbitrary screen-space rectangle inside a `Screen` is the "picture-in-picture" family: `GuiGraphicsExtractor.skin(Model.Simple model, Identifier texture, float scale, float rotationX, float rotationY, float pivotY, int x0, int y0, int x1, int y1)` (also `.entity(...)`, `.book(...)`, `.bannerPattern(...)`) — confirmed via `javap -p` against the resolved 26.2 jar (no decompiled source was cached locally); note the **method's own float-parameter order does not match its backing `GuiSkinRenderState` record's field order** (method: `scale, rotationX, rotationY, pivotY`; record fields: `rotationX, rotationY, pivotY, ..., scale`), confirmed by reading the bytecode's local-variable load order at the call site, not just the method signature. Because this whole model is "record a descriptor, render it later," each `skin(...)`/`entity(...)` call is internally isolated by Mojang's own `GuiRenderState`/`PictureInPictureRenderState` machinery — **no manual `PoseStack` push/pop is needed (or possible) to make it coexist with 2D `GuiGraphicsExtractor` calls in the same frame**, unlike the pre-26.x model. | Not applicable — 1.21.11 still uses the older immediate-mode `Screen.render(GuiGraphics, int, int, float)` model with real `PoseStack`/`VertexConsumer` access, so a genuinely custom 3D scene/camera is straightforward there by comparison. | Confirmed on **26.2**; not yet re-verified on 26.1 (same Mojang-mapping family, likely identical — re-confirm when porting). | `main-menu` (`MainMenuBackgroundRenderer`, Sequencing step 4 spike) |
| **The base `Entry` type itself (`AbstractSelectionList.Entry` / `EntryListWidget.Entry`) is `protected`, not just its `addEntry`/`clearEntries` methods** — a genuine compile-time blocker the plan's own working assumption did not anticipate (it only flagged the *methods* as protected). Only visible via `javap -v`'s `InnerClasses` attribute (`protected static abstract ... Entry=class ...`); plain, non-verbose `javap` run directly against the nested class file misleadingly reports the class itself as `public` (the standalone `.class` file's own access flag differs from the modifier recorded in the enclosing class's `InnerClasses` table, which is what javac actually enforces at compile time — confirmed the hard way, via a real `javac`/Loom compile failure: `"Entry has protected access in AbstractSelectionList"`) | `EntryListWidget.Entry` is `protected`; the concrete `WorldListWidget.Entry` (a subtype, declared directly on `WorldListWidget`) is `public` | `AbstractSelectionList.Entry` is `protected`; the concrete `WorldSelectionList.Entry` (a subtype, declared directly on `WorldSelectionList`) is `public` | `steam-cloud-sync` (`WorldSelectionListInvokerMixin`/`WorldListWidgetInvokerMixin`, implementation — real Gradle compile) |
| A save's persisted stats-file directory (`stats/<uuid>.json`), resolved from a `LevelStorageSource`/`LevelStorage` session opened by save-folder name -- no reusable "load a StatsCounter/StatHandler from a save's stats file" API was found on either mapping, so both versions hand-parse the raw JSON directly instead | `net.minecraft.client.MinecraftClient.getLevelStorage(): LevelStorage`; `LevelStorage.createSession(String): LevelStorage.Session` (throws `IOException`, `net.minecraft.util.path.SymlinkValidationException`); `LevelStorage.Session.getDirectory(WorldSavePath): Path`, enum constant `WorldSavePath.STATS`. Player UUID for the file name: `MinecraftClient.getInstance().getSession().getUuidOrNull()`. | `net.minecraft.client.Minecraft.getLevelSource(): LevelStorageSource`; `LevelStorageSource.createAccess(String): LevelStorageSource.LevelStorageAccess` (`AutoCloseable`, throws `IOException`); `LevelStorageAccess.getLevelPath(LevelResource): Path`, enum constant `LevelResource.PLAYER_STATS_DIR` (**not** `STATS_DIR` -- confirmed via `javap` against the resolved 26.1 jar after an initial wrong guess). Player UUID: `Minecraft.getInstance().getUser().getProfileId()`. Identical between 26.1 and 26.2. | `main-menu` (batch-3-fixes BF2, `StatisticsPanel.reload()` disk-scan rework) |
| **Superseded — three consecutive Mixin-based designs for exposing a target's own protected `addEntry(E)`/`clearEntries()` all failed for different confirmed reasons; the actual working fix abandons `@Mixin`/`@Invoker` for this specific problem entirely and uses plain reflection instead.** (1) A plain `interface` naming the invoker's parameter as the concrete public `Entry` *subtype* (e.g. `WorldSelectionList.Entry`) compiles, but produces the wrong compiled descriptor — the real method erases its parameter to the *base* `AbstractSelectionList$Entry`/`EntryListWidget$Entry`, not the subtype — a real in-game `InvalidAccessorException` crash on *every* screen using any vanilla scrolling list. (2) Fixing the descriptor by declaring the parameter as the real, protected base `Entry` type requires an abstract class **extending the mixin's own exact `@Mixin` target** (only real subclass privilege can name a protected type from outside its package) plus a separate "duck interface" for callers to cast to (a `class`-to-unrelated-`class` cast needs a real compile-time sub/supertype relationship a mixin class never has with its merge target). This compiles and *sometimes* boots without visible errors, but Sponge Mixin's own hierarchy validator rejects a mixin class declaring `extends TargetItself` — confirmed via a real, reproducible in-game `InvalidMixinException: Super class 'X' ... was not found in the hierarchy of target class 'X'`, present on **every** platform module (unobfuscated or not), not just the remapped one — it only stayed silent until something actually tried to use the mixin. (3) Declaring the `@Invoker` interface directly inside the target's own package (`net.minecraft.client.gui.components`/`...widget`) instead of `de.lazuli.mixin` grants same-package protected access with no `extends` clause needed at all, sidestepping (2)'s validator bug — but Sponge Mixin's mixin-package-ownership mechanism claims the **entire declared package**, not just the mixin classes inside it: a real, immediate crash trying to load vanilla's own unrelated `Renderable`, itself also in that same vanilla package. **The fix that actually works**: a plain reflection helper (`AbstractSelectionListReflection`/`EntryListWidgetReflection`, `de.lazuli.cloudsync`) that finds `addEntry`/`clearEntries` via `getDeclaredMethods()` filtered by name+arity and calls `setAccessible(true)` + `invoke(...)` — no Java source-level type is ever written for the protected `Entry` parameter (an `Object` argument to `Method.invoke`), so there is no descriptor to get wrong, no type to gain access to via inheritance, and no Mixin package-ownership question. `setAccessible(true)` works here because Fabric Loader's classloader does not run mods under strict JPMS encapsulation. Bonus finding: mixin (1)/(2)'s hierarchy-validator failure was also silently breaking **refmap generation for the entire mixin config file it shared with**, causing an unrelated, otherwise-correct `@Shadow`-based mixin in the same config to fail with `"No refMap loaded"` — removing the broken invoker mixin fixed that mixin too, with zero changes to it. | Same mechanism/constraint at every step (none of this is mapping-specific) | Same mechanism/constraint at every step | `steam-cloud-sync` (`WorldSelectionListInvokerMixin` → `WorldSelectionListDuck` → package-squat → `AbstractSelectionListReflection`/`EntryListWidgetReflection`; three consecutive in-game crashes across two manual-verification passes) |
| World edit/rename navigation-on-close: how a "Save"/"Cancel" screen transition is wired for the world-edit flow — **not just a rename, a genuinely different call shape/ownership model** | This platform never calls `EditWorldScreen.create(...)` directly with a hand-supplied callback; instead a headless `WorldListWidget` is built via `new WorldListWidget.Builder(MinecraftClient, owner).toWidget()` (`owner` = the hosting `MainMenuScreen`), and the real per-row `WorldListWidget.WorldEntry.edit()` (`public`, confirmed via `javap`) is called directly — vanilla's own button-press implementation already navigates back to the `Builder`'s `owner` screen on both Save and Cancel internally, since that's the parent screen baked into the widget at construction time. No no-op callback exists to fix here. | `EditWorldScreen.create(Minecraft, LevelStorageSource.LevelStorageAccess, BooleanConsumer callback)` takes an explicit `BooleanConsumer` the caller must supply directly (no owning list/parent-screen indirection); `onClose()` (Cancel/ESC) is confirmed via `javap` to be exactly `callback.accept(false)` with no screen transition of its own — the caller-supplied callback is the *only* place navigation can happen, so a no-op callback (as `WorldsPanel.editWorld` originally passed) silently breaks both Save and Cancel. Confirmed identical on both 26.1 and 26.2 (same call shape). | `main-menu` (post-launch-fixes-2 FX15, `WorldsPanel.editWorld`) |
| Per-row rendering/click-handling injected into an **existing, real** list entry (not a brand-new synthetic row, contrast with the row above) — needed for a per-world toggle icon drawn *inside* the vanilla world-row layout rather than as a separate overlay widget. The concrete, real per-world row class is `public final` on both sides (unlike the abstract `Entry` base), so **no duck-interface split is needed here** — a plain `@Inject`-style `@Mixin` targeting the concrete row class can call the row's own already-public methods (`getX()`/`getY()`/`getWidth()`/`getHeight()`, `getLevelSummary()`/`getLevel()`) directly from an injected handler, since `this` inside an injected method is the real merged instance. Render method: takes `(graphics, mouseX, mouseY, hovering, partialTick)` — no explicit bounds params; use the row's own bounds accessors instead. Click method: takes `(clickEvent, doubleClick)` — an event *record* with `.x()`/`.y()`/`.button()`, not raw `(double, double, int)` on either version. A plain `graphics.fill(x1, y1, x2, y2, argbColor)` call (already the established pattern in this codebase, `CloudOnlyWorldListEntry`) avoids needing to register a real texture/sprite (which requires a `RenderPipeline` argument on ≥26.1's `blitSprite`) for a simple colored-indicator icon. | Concrete row class `net.minecraft.client.gui.screen.world.WorldListWidget.WorldEntry` (`public final`); render method `render(net.minecraft.client.gui.DrawContext, int, int, boolean, float)`; click method `mouseClicked(net.minecraft.client.gui.Click, boolean)`, `Click` is a record (`x()`/`y()`/`button()`); world-name/id accessor `getLevel()` / `getLevelDisplayName()` | Concrete row class `net.minecraft.client.gui.screens.worldselection.WorldSelectionList.WorldListEntry` (`public final`); render method `extractContent(net.minecraft.client.gui.GuiGraphicsExtractor, int, int, boolean, float)`; click method `mouseClicked(net.minecraft.client.input.MouseButtonEvent, boolean)`, `MouseButtonEvent` is a record (`x()`/`y()`/`button()`); world-name/id accessor `getLevelSummary()` (returns `LevelSummary`, itself has `getLevelId()`/`getWorldName()`/etc.) | `steam-cloud-sync` (Group 6 per-row sync-toggle icon redesign, requested after manual verification found the v1 implementation used a single global footer button instead) |
| **Fix for the above (javac-level only):** an `@Invoker` method's declared parameter type does not need to exactly match the real (here, inaccessible-to-us) target parameter type at the *source* level — declaring it as a more specific, **accessible** subtype (here, the concrete list's own public `Entry` subclass) compiles cleanly on both sides. On 26.x/26.1 (no remap step — plain `net.fabricmc.fabric-loom`, Mojang-mapped) this is also confirmed sufficient end-to-end: a real `:platform:fabric-26.2:compileJava`/`:fabric-26.1:compileJava` run succeeds with zero warnings. | `int lazuli$invokeAddEntry(WorldListWidget.Entry entry)` (not `EntryListWidget.Entry<?>`) | `int lazuli$invokeAddEntry(WorldSelectionList.Entry entry)` (not `AbstractSelectionList.Entry<?>`) | `steam-cloud-sync` (implementation) |
| **Open item (1.21.11 only, unresolved by this implementation pass):** on the sole platform module using `fabric-loom-remap`'s real obfuscated-Minecraft remap pipeline, a real `:platform:fabric-1.21.11:remapJar` run prints `"Cannot remap addEntry because it does not exist in any of the targets [net/minecraft/client/gui/widget/EntryListWidget] or their parents"` for the `addEntry` invoker specifically — never for `clearEntries` (arity 0, no generic-erased parameter). Confirmed *not* fixable by choice of our own declared parameter type: tried both the concrete public `WorldListWidget.Entry` subtype and plain `Object` — identical warning either way, ruling out a source-level fix. Matches a documented, known class of Fabric Loom/tiny-remapper limitation remapping `@Invoker`/`@Accessor` targets whose real parameter is a generic type variable (see FabricMC/tiny-remapper issues #124/#126) — not specific to this mixin. The overall build still succeeds (warning, not a failure) and the mixin class is confirmed packaged into the built jar; whether Sponge Mixin's own runtime resolution (independent of this build-time pre-remap pass) still correctly applies `addEntry` against a real, launched, obfuscated 1.21.11 client has **not** been confirmed — this is the single highest-priority check for the verification phase to run first for Group 6 on 1.21.11. | `EntryListWidget.addEntry(E)` — generic parameter, remap warning present (see above) | N/A — no remap step exists on this side at all (plain `jar`, not `remapJar`); not applicable | `steam-cloud-sync` (implementation, real `remapJar` run) |
| Screen rendering entry point (a custom `Screen` subclass overriding its own drawing, e.g. a manually-drawn progress bar) -- **class name, method name, and drawing model all diverge, not just a rename**, confirmed via `javap` against this repo's actual resolved jars | `Screen.render(net.minecraft.client.gui.DrawContext context, int mouseX, int mouseY, float delta)`; draw via `context.fill(x1, y1, x2, y2, color)` / `context.drawCenteredTextWithShadow(textRenderer, text, x, y, color)`; button widget class is `net.minecraft.client.gui.widget.ButtonWidget` | `Screen.extractRenderState(net.minecraft.client.gui.GuiGraphicsExtractor guiGraphics, int mouseX, int mouseY, float delta)` -- Mojang's 26.x mapping renamed the whole immediate-mode `render`/`GuiGraphics` model to an "extract render state" model (`GuiGraphicsExtractor`, `extractRenderState`/`extractWidgetRenderState`/`extractContent` across every widget/list class); draw via the same-shaped `guiGraphics.fill(x1, y1, x2, y2, color)` / `guiGraphics.centeredText(font, component, x, y, color)` methods, still present but on the renamed/refactored type; button widget class is `net.minecraft.client.gui.components.Button` | `steam-cloud-sync` (`WorldRestoreScreen`, implementation) |
| `LevelSummary`'s own accessors for a world's save-folder slug / display name (used to key per-world Cloud sync state) -- same class package/name on both sides (`net.minecraft.world.level.storage.LevelSummary`), but its own method names diverge, confirmed via `javap` | `getName()` (save-folder slug) / `getDisplayName()` (player-facing name) | `getLevelId()` (save-folder slug) / `getLevelName()` (player-facing name) | `steam-cloud-sync` (`FabricWorldSyncToggleInjector`/`FabricCloudOnlyWorldListInjector`, implementation) |
| Queuing a simple text tooltip from inside a Pattern-3 `@Inject` render handler (e.g. a per-world status icon's hover tooltip) -- confirmed via `javap` against this repo's actual resolved jars (`net.minecraft.client.gui.DrawContext` for 1.21.11 Yarn; `net.minecraft.client.gui.GuiGraphicsExtractor` for both 26.1 and 26.2, identical method set on both) | `net.minecraft.client.gui.DrawContext.drawTooltip(net.minecraft.text.Text text, int mouseX, int mouseY)` | `net.minecraft.client.gui.GuiGraphicsExtractor.setTooltipForNextFrame(net.minecraft.network.chat.Component text, int mouseX, int mouseY)` -- identical signature/behavior on both 26.1 and 26.2 | `steam-cloud-sync` (diagnostics/UI/compression companion spec, per-world sync-status icon tooltip, implementation) |
| Client-side singleplayer-vs-multiplayer check (FR4.1's `LastPlayedPointer.type`/FR6.2 world-unload detection) -- confirmed via `javap`, a real divergence beyond a simple rename (different method names, not just remapped) | `MinecraftClient.isIntegratedServerRunning()` / `MinecraftClient.getServer()` returning `net.minecraft.server.integrated.IntegratedServer` (nullable); currently-joined server via `MinecraftClient.getCurrentServerEntry()` returning `net.minecraft.client.network.ServerInfo` | `Minecraft.hasSingleplayerServer()` / `Minecraft.getSingleplayerServer()` returning `net.minecraft.server.MinecraftServer` (its `IntegratedServer` subtype, nullable); currently-joined server via `Minecraft.getCurrentServer()` returning `net.minecraft.client.multiplayer.ServerData` | `steam-cloud-sync` (`SteamCloudSyncClientInitializer`, implementation) |
| Resolving a singleplayer world's own save-folder path from `MinecraftServer` (needed for Group 6's world-unload archive trigger, FR6.2) | `MinecraftServer.getSavePath(net.minecraft.util.WorldSavePath.ROOT)`; display name via `MinecraftServer.getSaveProperties().getLevelName()` | `MinecraftServer.getWorldPath(net.minecraft.world.level.storage.LevelResource.ROOT)`; display name via `MinecraftServer.getWorldData().getLevelName()` | `steam-cloud-sync` (`SteamCloudSyncClientInitializer`, implementation) |
| The main options screen / the Realms main screen -- concrete package names diverge from planning-time guesses on both sides, confirmed via real `:platform:...:compileJava` runs (Yarn side additionally diverges from the spec/plan's own guessed package), not `javap`/jar-entry-only guessing | `OptionsScreen` is `net.minecraft.client.gui.screen.option.OptionsScreen` (plan's guess was correct); `RealmsMainScreen` is `net.minecraft.client.realms.gui.screen.RealmsMainScreen` -- **not** `net.minecraft.realmsclient.gui.screens.RealmsMainScreen` the plan guessed (no `net.minecraft.realmsclient` package exists on this side; Realms lives under `net.minecraft.client.realms` here) | `OptionsScreen` actually lives in `net.minecraft.client.gui.screens.options.OptionsScreen`, not `net.minecraft.client.gui.screens.OptionsScreen`; `RealmsMainScreen` actually lives in `com.mojang.realmsclient.RealmsMainScreen`, not `net.minecraft.realmsclient.gui.screens.RealmsMainScreen` (no `net.minecraft.realmsclient` package exists at all -- Realms classes are under Mojang's own `com.mojang.realmsclient` namespace, not remapped into `net.minecraft`) | `friends-sidebar` (`FabricFriendsSidebarInjector`, implementation, real compile) |
| Avatar/dynamic-texture upload from raw RGBA bytes -- class names and per-pixel color-channel order both diverge, confirmed via real compile against each side's resolved jar | `com.mojang.blaze3d.platform.NativeImage`; per-pixel upload is ABGR-only (`NativeImage.setPixelABGR(x, y, abgr)`/`setPixel(x, y, abgr)`, no ARGB convenience overload exists); texture class is `net.minecraft.client.renderer.texture.DynamicTexture` (a concrete class), constructed as `new DynamicTexture(Supplier<String>, NativeImage)`, `.upload()`; registered via `TextureManager.register(Identifier, AbstractTexture)` | `net.minecraft.client.texture.NativeImage`; per-pixel upload has an ARGB convenience method, `NativeImage.setColorArgb(x, y, argb)` (also a raw `setColor(x, y, ...)`); texture class is `net.minecraft.client.texture.NativeImageBackedTexture` (26.x's `DynamicTexture` name is instead an *interface* this class implements), constructed as `new NativeImageBackedTexture(Supplier<String>, NativeImage)`, `.upload()`; registered via `TextureManager.registerTexture(Identifier, AbstractTexture)` | `friends-sidebar` (`AvatarTextureCache`, implementation, real compile) |
| `ClientPlayConnectionEvents`' connection-handle parameter type (event class/package itself, `net.fabricmc.fabric.api.client.networking.v1.ClientPlayConnectionEvents` in module `fabric-networking-api-v1`, is the same simple name/package on both sides of the boundary — Fabric API's own classes are not remapped the way Minecraft's own classes are) — **Yarn side now confirmed via `javap` against this repo's actual remapped 1.21.11 `fabric-networking-api-v1` jar** (`.gradle/loom-cache/remapped_mods/remapped/net/fabricmc/fabric-api/fabric-networking-api-v1-*/...jar`), matching the planning-time recollection exactly (no divergence found) | `net.minecraft.client.network.ClientPlayNetworkHandler` | `net.minecraft.client.multiplayer.ClientPacketListener` (confirmed via direct fetch of Fabric API's own GitHub source at tag `0.141.4+1.21.11`, which is itself written against Mojang-mapped names; independently reconfirmed via `javap` against the real resolved 26.2 `fabric-api` jar's nested `fabric-networking-api-v1` module) | `steam-cloud-sync` (implementation) |
| A custom widget's own mouse-scroll-wheel hook (needed for `friends-sidebar` v1.1's scrollable friends list, FR4.7) — **confirmed identical on both sides via `javap`** against each side's own resolved Minecraft jar (`net.minecraft.client.gui.components.events.GuiEventListener` on 26.x, `net.minecraft.client.gui.Element` on 1.21.11's Yarn side — both interfaces `AbstractWidget`/`ClickableWidget` already implement), no divergence found, no fallback needed | `public default boolean mouseScrolled(double mouseX, double mouseY, double horizontalAmount, double verticalAmount)` on `net.minecraft.client.gui.Element` | `public default boolean mouseScrolled(double mouseX, double mouseY, double horizontalAmount, double verticalAmount)` on `net.minecraft.client.gui.components.events.GuiEventListener` | `friends-sidebar` v1.1 (`FriendSidebarWidget`, implementation, `javap` pass) |
| **Correction to an earlier (wrong) assumption in this same table's history**: `drawText`/`drawTextWithShadow` on the Yarn/1.21.11 side has the **same** alpha-zero-skip pitfall as `GuiGraphicsExtractor.text()` on 26.x — it was previously assumed immune (never actually `javap`-verified) because it takes a plain `int` color rather than an ARGB-named parameter. Confirmed via `javap -c`: `DrawContext.drawText(...)` calls `ColorHelper.getAlpha(color)` and returns immediately, drawing nothing, if the alpha byte is `0` — identical semantics to the 26.x side. A real symptom: `friends-sidebar` v1.1's row status text used `statusColor & 0xFFFFFF` (explicitly masking alpha to 0) and rendered nothing at all on 1.21.11 while working fine on 26.x, until fixed | `net.minecraft.client.gui.DrawContext.drawText(TextRenderer, OrderedText/Text/String, int x, int y, int color, boolean shadow)` — silently no-ops if `ColorHelper.getAlpha(color) == 0`; colors must carry a real `0xFF` alpha byte (e.g. `0xFFFFFFFF`, not `0xFFFFFF`) | N/A (26.x's own `GuiGraphicsExtractor.text()` alpha-zero pitfall already documented elsewhere in this session's fixes) | `friends-sidebar` v1.1 (`FriendSidebarWidget`/`FriendContextMenuWidget`, manual in-game verification after user report: "1.21.11 is displaying no text") |
| `SteamUser`/`SteamFriends` v1.1 rich-presence/local-identity surface (`SteamUser.getSteamID()`, `SteamFriends.getPersonaName()`/`getPersonaState()` no-arg self overloads, `getFriendRichPresence(SteamID, String)`, `requestFriendRichPresence(SteamID)`) — **all confirmed present with exactly the signatures the spec/plan cited via `javap -p` against this repo's own resolved `steamworks4j-1.10.0.jar`** (`platform/fabric-26.2/build/processIncludeJars/steamworks4j-1.10.0.jar`); `SteamUserCallback` is an interface with every method declared `default`, so a no-op `implements SteamUserCallback {}` inner class needs zero method overrides | steamworks4j is not per-Minecraft-version (one shared jar, Jar-in-Jar'd into every platform module) — identical on both sides | steamworks4j is not per-Minecraft-version (one shared jar, Jar-in-Jar'd into every platform module) — identical on both sides | `friends-sidebar` v1.1 (`FriendsService`, implementation, mandatory pre-code `javap` pass) |
| **Trap, not a rename — queuing a native tooltip (`setTooltipForNextFrame`/`drawTooltip`) from inside `ScreenEvents.afterExtract`/`afterRender` never actually renders, on *either* mapping side.** Confirmed via `javap -c` against both `fabric-screen-api-v1`'s mixin sources and each side's own resolved Minecraft jar: the vanilla method that wraps `ScreenEvents.afterExtract`/`afterRender` (`Screen.extractRenderStateWithTooltipAndSubtitles`/`Screen.renderWithTooltipAndSubtitles`) itself calls the tooltip-queue-to-render flush (`GuiGraphicsExtractor.extractDeferredElements(mouseX,mouseY,delta)` / `DrawContext.drawDeferredElements()`) as its own *last* step, and `fabric-screen-api-v1`'s wrapping mixin (`GuiMixin`'s `@WrapOperation` on 26.x; `GameRendererMixin`'s `@WrapOperation` on 1.21.11) fires `afterExtract`/`afterRender` only *after* that whole wrapped call — including that last flush step — returns. So any `setTooltipForNextFrame`/`drawTooltip` call made from inside an `afterExtract`/`afterRender` handler queues state that this frame's flush already consumed; it is silently never drawn, every frame (not merely delayed one frame — permanently lost). **Fix**: from inside the `afterExtract`/`afterRender` handler itself, after all of that frame's own draw calls that might set a tooltip, manually re-invoke the same flush method again (`guiGraphics.extractDeferredElements(mouseX, mouseY, delta)` / `context.drawDeferredElements()`) — confirmed via `javap -c` to be idempotent/safe to call twice per frame (it nulls its own backing field after running, no-ops if null). | `net.minecraft.client.gui.DrawContext.drawDeferredElements()` (no params); wrapped by `fabric-screen-api-v1`'s `GameRendererMixin` around `Screen.renderWithTooltipAndSubtitles` | `net.minecraft.client.gui.GuiGraphicsExtractor.extractDeferredElements(int mouseX, int mouseY, float delta)`; wrapped by `fabric-screen-api-v1`'s `GuiMixin` around `Screen.extractRenderStateWithTooltipAndSubtitles` — identical on both 26.1 and 26.2 | `friends-sidebar` "Polish pass" dropdown redesign bug fix (`DropdownWidget`/`FabricFriendsSidebarInjector`, manual in-game verification after user report: "native tooltip no longer appears") |
| Integrated-server Netty/login networking stack (mixin targets for Steam World Hosting) — **whole-class + method-name renames across the boundary, confirmed via `javap`/`javap -c` against this repo's own resolved jars.** The client-connect `connect` static builder additionally has a **different arg type** and (Yarn only) is an **ambiguously overloaded** name needing a full-descriptor mixin selector; `initServer`/`isPublished`/`getRemoteAddress`/`setEncryptionKey`/`Crypt.digestData`/`Crypt.getCipher`/`ServerboundKeyPacket` all rename on the Yarn side. Inner `io.netty` `ServerBootstrap.childHandler/group` + `Bootstrap.channel/group/connect(InetAddress,int)` calls are identical on both sides (target with `remap=false`). | Server listener `net.minecraft.server.ServerNetworkIo`, bind method `bind(InetAddress,int)`, reached via `MinecraftServer.getNetworkIo()`. Connection `net.minecraft.network.ClientConnection`; static builder `connect(InetSocketAddress, net.minecraft.network.NetworkingBackend, ClientConnection)` returning `ChannelFuture` (**overloaded** name — mixin `method` needs the full descriptor to disambiguate); remote addr `getAddress()`; disable-encryption `setupEncryption(Cipher,Cipher)`. `IntegratedServer.setupServer()` (world-load), `isRemote()` (pause-suppress flag). Login handlers `client.network.ClientLoginNetworkHandler#onHello` / `server.network.ServerLoginNetworkHandler#onHello`/`onKey`; crypto `net.minecraft.network.encryption.NetworkEncryptionUtils.computeServerId(...)`/`cipherFromKey(int,Key)`; key packet `packet.c2s.login.LoginKeyC2SPacket` (`verifySignedNonce`/`decryptSecretKey`, ctor calls `NetworkEncryptionUtils.encrypt(Key,[B)`). Client-connect UI `client.gui.screen.multiplayer.ConnectScreen.connect(Screen, MinecraftClient, ServerAddress, ServerInfo, boolean, CookieStorage)`; `ServerInfo(String,String,ServerInfo.ServerType.OTHER)`; `ServerAddress.parse(String)`. | Server listener `net.minecraft.server.network.ServerConnectionListener`, bind method `startTcpServerListener(InetAddress,int)`, reached via `MinecraftServer.getConnection()`. Connection `net.minecraft.network.Connection`; static builder `connect(InetSocketAddress, net.minecraft.server.network.EventLoopGroupHolder, Connection)` returning `ChannelFuture` (**unambiguous** `connect` name — bare `method="connect"` suffices); remote addr `getRemoteAddress()`; disable-encryption `setEncryptionKey(Cipher,Cipher)` (no `encrypted` field to `@Shadow` — check the channel type instead). `IntegratedServer.initServer()` (world-load), `isPublished()` (pause-suppress flag). Login handlers `client.multiplayer.ClientHandshakePacketListenerImpl#handleHello` / `server.network.ServerLoginPacketListenerImpl#handleHello`/`handleKey`; crypto `net.minecraft.util.Crypt.digestData(...)`/`getCipher(int,Key)`; key packet `network.protocol.login.ServerboundKeyPacket` (`isChallengeValid`/`getSecretKey`, ctor calls `Crypt.encryptUsingKey(Key,[B)`). Client-connect UI `client.gui.screens.ConnectScreen.startConnecting(Screen, Minecraft, ServerAddress, ServerData, boolean, TransferState)`; `ServerData(String,String,ServerData.Type.OTHER)`; `ServerAddress.parseString(String)`. Identical class/method set across `26.1` and `26.2`. | `steam-world-hosting` (implementation, mandatory-first-step `javap` pass across all three resolved jars) |
| `Villager`/`AbstractMinecart`/`AbstractBoat` package locations -- **genuine sub-package restructuring on the Mojang-mapped side, not just a class rename**, confirmed via `javap -p`/jar-entry listing against this repo's own resolved jars for all three modules. The spec/plan's own illustrative guesses (`net.minecraft.world.entity.npc.Villager`, `net.minecraft.world.entity.vehicle.AbstractMinecart`/`Boat`, flat packages) do not exist on 26.1/26.2 -- both moved into new leaf sub-packages during Mojang's own internal reorganization. Identical on 26.1 and 26.2 (same sub-package layout on both). Yarn/1.21.11 keeps the older flat-package shape. | `net.minecraft.entity.passive.VillagerEntity`; `net.minecraft.entity.vehicle.AbstractMinecartEntity`; boat check uses the common base `net.minecraft.entity.vehicle.AbstractBoatEntity` (not the concrete `BoatEntity`, so chest boats etc. also match) | `net.minecraft.world.entity.npc.villager.Villager` (not `entity.npc.Villager`); `net.minecraft.world.entity.vehicle.minecart.AbstractMinecart` (not `entity.vehicle.AbstractMinecart`); `net.minecraft.world.entity.vehicle.boat.AbstractBoat` (not `entity.vehicle.Boat`) | `rich-presence` (implementation, mandatory-first-step `javap` pass for `PresenceSignalGatherer`'s vehicle/villager detection) |
| Bounded-radius "block entities near a position, already-loaded chunks only" query -- **a single clean method exists on both mapping sides, no fallback per-block scan needed** (the plan's Risk 3 flagged this as unconfirmed; now resolved). `Level.getChunk(int chunkX, int chunkZ)` / `World.getChunk(int, int)` returns the chunk object directly (present, not lazily faulted) and its `getBlockEntities()` returns that chunk's own small `Map<BlockPos, BlockEntity>` -- iterating a 5x5 chunk neighborhood (radius-in-chunks = `ceil(radiusBlocks/16)+1`) and filtering that map's values by `instanceof` is cheap and needs no raw per-block-position iteration at all. | `net.minecraft.world.chunk.WorldChunk.getBlockEntities(): Map<BlockPos, BlockEntity>`, reached via `World.getChunk(int, int): WorldChunk` | `net.minecraft.world.level.chunk.LevelChunk.getBlockEntities(): Map<BlockPos, BlockEntity>`, reached via `Level.getChunk(int, int): LevelChunk` -- identical on 26.1/26.2 | `rich-presence` (implementation, Near-a-Village bell scan) |
| Registry-key-derived translation key for a `Holder`/`RegistryEntry`-wrapped value (e.g. biome) -- **the accessor name itself diverges beyond a simple rename** on the Mojang-mapped side (newer Mojang mapping renamed `ResourceKey.location()` to `.identifier()`; confirmed via `javap -p`, no `location()` overload remains at all on 26.1/26.2) | `RegistryEntry<Biome>.getKey(): Optional<RegistryKey<Biome>>`; `RegistryKey.getValue(): Identifier` (still named `getValue`/returns `net.minecraft.util.Identifier`) | `Holder<Biome>.unwrapKey(): Optional<ResourceKey<Biome>>`; `ResourceKey.identifier(): Identifier` (returns `net.minecraft.resources.Identifier`) -- **not** `.location()`, which does not exist on this mapping | `rich-presence` (implementation, biome-translation-key derivation) |
| "Return to title screen" choke-point method to redirect for a full-screen-replacement feature (FR1.2-shaped: intercepting every vanilla path that would show `TitleScreen`) -- confirmed via `javap -p` against each side's own resolved Minecraft jar; **the owning class itself diverges, not just a method rename** | `net.minecraft.client.MinecraftClient.setScreen(Screen)` remains the single choke point directly on the client singleton -- `disconnect(Text)`/`disconnectWithSavingScreen()`/`disconnectWithProgressScreen()` and world-exit all funnel through it unchanged from the pre-26.x model | Screen-setting was refactored off `Minecraft` onto a separate `net.minecraft.client.gui.Gui` class -- `Minecraft.setScreenAndShow(Screen)` itself just delegates to `this.gui.setScreen(screen)`; redirect on `Gui.setScreen(Screen)`, not `Minecraft` | `main-menu` (`ClientTitleScreenRedirectMixin`/`GuiTitleScreenRedirectMixin`, FR1.2 disconnect/world-exit wiring, Sequencing step 13 porting) |
| Accessing the currently-active `Screen` from outside a `Screen` subclass (needed for a sidebar-owned button to open `OptionsScreen` with the right "last screen" to return to) -- **diverges even between the two 26.x builds, not just Yarn vs. Mojang**, confirmed via a real compile failure on 26.1 after code that worked on 26.2 | `MinecraftClient.currentScreen` -- public field directly on the client singleton | **26.1**: `Minecraft.screen` -- a public field directly on the client singleton (same shape as Yarn, just Mojmap-named). **26.2**: the field was refactored off `Minecraft` onto `Gui` -- `Minecraft.getInstance().gui.screen()`, a method, not a field (`Minecraft` itself has no `screen` field/method at all on this side; confirmed via `javap`, matching the same `Minecraft`→`Gui` migration already noted above for the toast manager and title-screen redirect). | `main-menu` (`FriendSidebarWidget`'s Options/Quit footer, real compile failure on 26.1 after code that compiled fine on 26.2) |
| `LevelStorageSource.LevelStorageAccess` lifecycle ownership when opening `EditWorldScreen` -- **not documented in the constructor/factory, only discoverable via `javap`-reading `onClose()`'s actual bytecode**; a real in-game bug (`OverlappingFileLockException`, world silently vanishing from the reloaded list) resulted from assuming the screen closes its own access | `EditWorldScreen`'s vanilla caller-callback shape is unused on this platform at all (see the FX15 divergence row above -- this platform drives edit via `WorldListWidget.WorldEntry.edit()`, which manages its own `LevelStorageAccess` lifecycle internally); not applicable here | `EditWorldScreen.create(Minecraft, LevelStorageAccess, BooleanConsumer)` never closes the `LevelStorageAccess` it's handed -- confirmed via `javap -c`: neither `onClose()` nor the Save path (`onRename`) call `.close()` anywhere in the class. The **caller** that constructed the access via `LevelStorageSource.createAccess(...)` owns its lifecycle and must close it itself (e.g. in the completion callback) before doing anything that re-acquires the same world's directory lock (like reloading the world list), or the lock is still held and the reload throws `OverlappingFileLockException`, silently dropping that world from the reloaded results. | `main-menu` (post-launch-fixes-3, `WorldsPanel.editWorld` world-vanishes-after-Cancel bug, `javap` bytecode read) |
| Cleanly exiting the client entirely ("Quit to Desktop") from custom UI -- no vanilla button exercises this path today on either side (only exercised by the window-close handler), confirmed via `javap` method-listing against each side's own resolved jar | `MinecraftClient.scheduleStop()` -- the safer, defer-to-next-tick variant is what's exposed/idiomatic here (an immediate synchronous `stop()` also technically exists but isn't the one the window-close path prefers) | `Minecraft.stop()` only -- confirmed via `javap` that no `scheduleStop()` overload exists on either 26.1 or 26.2's `Minecraft` class; `stop()` is the only shutdown entry point available | `main-menu` (`FriendSidebarWidget`'s Options/Quit footer, "Quit to Desktop" button) |
| Vanilla player-statistics API (`Stat`/`StatType`/the per-player counter holder, plus how a player object exposes it) -- **whole-package rename plus per-constant renames, confirmed via `javap -p` against each of the three resolved Minecraft jars**; shape (registry-style `StatType<T>` holding `Stat<T>` instances, `getOrCreateStat(T)`/`get(T)` to obtain one, an `Object2IntMap`-backed counter class with a `getStat`/`getValue(Stat<?>)` int accessor) is identical on both sides, only names diverge | Package `net.minecraft.stat`; `Stats.MINED`/`CRAFTED`/`USED`/`BROKEN`/`PICKED_UP`/`DROPPED`/`KILLED`/`KILLED_BY`/`CUSTOM` (`StatType`s); `StatType.getOrCreateStat(T)`; per-player counter class `net.minecraft.stat.StatHandler`, `getStat(StatType<T>, T)`/`getStat(Stat<?>)`; obtained via `ClientPlayerEntity.getStatHandler()` (`MinecraftClient.player` field) | Package `net.minecraft.stats`; `Stats.BLOCK_MINED`/`ITEM_CRAFTED`/`ITEM_USED`/`ITEM_BROKEN`/`ITEM_PICKED_UP`/`ITEM_DROPPED`/`ENTITY_KILLED`/`ENTITY_KILLED_BY`/`CUSTOM`; `StatType.get(T)`; per-player counter class `net.minecraft.stats.StatsCounter`, `getValue(StatType<T>, T)`/`getValue(Stat<?>)`; obtained via `LocalPlayer.getStats()` (`Minecraft.player` field). Identical on both 26.1 and 26.2. Registry-iteration source for summing a stat across every block/item/entity type also renames: `net.minecraft.registry.Registries.{BLOCK,ITEM,ENTITY_TYPE}` (Yarn) vs. `net.minecraft.core.registries.BuiltInRegistries.{BLOCK,ITEM,ENTITY_TYPE}` (Mojmap). | `cross-world-stats` (implementation, mandatory-first-step `javap` pass across all three resolved jars, `CrossWorldStatsMergeHook`) |
| steamworks4j vendored `1.10.0` networking surface — **legacy `SteamNetworking`/`ISteamNetworking` P2P only; the newer `ISteamNetworkingSockets`/`ISteamNetworkingMessages` surface is absent entirely** (confirmed via jar-entry search + upstream source at tag `1.10.0`). Legacy API: `sendP2PPacket(SteamID, ByteBuffer, P2PSend, channel)`, `isP2PPacketAvailable(channel, int[])`, `readP2PPacket(SteamID, ByteBuffer, channel)`, `acceptP2PSessionWithUser`/`closeP2PSessionWithUser`; callback `SteamNetworkingCallback.onP2PSessionRequest`/`onP2PSessionConnectFail` (both `default`). `SteamFriends` adds `setRichPresence`/`clearRichPresence`/`getFriendRichPresence`/`getFriendRelationship(id)==FriendRelationship.Friend`/`onGameRichPresenceJoinRequested` (all present). | steamworks4j is one shared Jar-in-Jar'd jar — identical on both sides | steamworks4j is one shared Jar-in-Jar'd jar — identical on both sides | `steam-world-hosting` (implementation; Networking surface verification) |
| Persistent-HUD-overlay registration API (drawing a widget every frame with no `Screen` open) — **`fabric-api`'s `net.fabricmc.fabric.api.client.rendering.v1.HudRenderCallback` (a single `Event<HudRenderCallback>`, `onHudRender(GuiGraphics/DrawContext, DeltaTracker)`) was replaced by a layered-registry API in newer `fabric-rendering-v1` releases**, confirmed via jar-entry listing (`unzip -l`) against each platform module's actually-resolved `fabric-rendering-v1` jar: present (`net/fabricmc/fabric/api/client/rendering/v1/HudRenderCallback.class`, `@Deprecated`) in the `16.2.10+0290ad933e` jar resolved for `fabric-1.21.11`; **absent** from the `23.0.4+c7e428fd47`/`25.3.1+6988455e9e` jars resolved for `fabric-26.1`/`fabric-26.2` — only the `net.fabricmc.fabric.api.client.rendering.v1.hud` package (`HudElementRegistry`/`HudElement`/`VanillaHudElements`) exists there. The two APIs are not call-compatible (different registration shape, different render-callback signature) — this is a genuine per-Fabric-API-version divergence, not a Minecraft-version rename. | `net.minecraft.client.gui.hud` equivalents unaffected — this row is about the **Fabric API** surface, not vanilla, so it applies identically regardless of Yarn/Mojang mapping; the deciding factor is which `fabric-rendering-v1` version each platform module's Loom-resolved `fabric-api` dependency pulls in | `HudElementRegistry.addLast(Identifier, HudElement)` (or `addFirst`/`attachElementBefore`/`attachElementAfter`) registers a `HudElement` (`void extractRenderState(GuiGraphicsExtractor, DeltaTracker)`) against a named layer, drawn every frame with no `Screen` open — the modern replacement for `HudRenderCallback.EVENT.register(...)` on `fabric-26.1`/`fabric-26.2` (both resolve the newer jar; `fabric-1.21.11` still resolves the older jar and keeps using the deprecated `HudRenderCallback` instead) | `main-menu` batch-2 (implementation, Item 1's `FabricFriendsSidebarInjector` global gameplay-overlay registration — first feature in this repo to use either API) |
| Opening a world's `level.dat` for a one-shot, batched raw-NBT read (needed for a conflict-resolution screen's seed/difficulty/cheats-enabled/day-count/version fields) -- **owning class and accessor method both diverge, not just a rename**, confirmed via `javap -p` against each module's own resolved Minecraft jar; both sides return the same DataFixerUpper `com.mojang.serialization.Dynamic<?>` (itself not a Minecraft-mapped type, so the *read* code -- `.get("Data").get(...).asNumber()/.asString()/.asBoolean()` -- is identical once you have the `Dynamic<?>` in hand) | `net.minecraft.world.level.storage.LevelStorage.Session` (obtained via `MinecraftClient.getInstance().getLevelStorage().createSession(String)`, `AutoCloseable`, throws `IOException`) has **no** `getUnfixedDataTagWithFallback`/`getSummary`-shaped method at all -- the equivalent is `Session.readLevelProperties(): Dynamic<?>` (also `readOldLevelProperties()` for the pre-backup copy) | `net.minecraft.world.level.storage.LevelStorageSource.LevelStorageAccess` (obtained via `Minecraft.getInstance().getLevelSource().createAccess(String)`, same shape as the stats-directory lookup already in this table) exposes `getUnfixedDataTagWithFallback(): Dynamic<?>` (also `getUnfixedDataTag(boolean)`) -- confirmed identical method set on 26.1 and 26.2 | `cloud-sync-conflict-ux` (`WorldsPanel.readLevelDatBatch`, implementation, mandatory `javap` pass per the plan's flagged risk) |
| Per-frame FOV computation choke point (needed to mixin-inject a zoom-style FOV override) — **the computation moved from `GameRenderer` to `Camera` entirely between mapping families, not just a rename**, confirmed via `javap -p`/`javap -v -p` against each platform's own resolved merged client jar (found already sitting in this repo's own `.gradle/loom-cache/minecraftMaven/net/minecraft/...`, no extra download needed). **Correction (see `docs/specs/tweaks-zoom-fov-fix.md`): on 26.1/26.2, `Camera.getFov()` is a dead accessor for this purpose — it is NOT on the render path.** Its only caller is `Camera.projectHorizonToScreen()` (an unrelated screen-space horizon helper), confirmed via `javap -c -p` on `Camera` and on `GameRenderer` (searching all `Camera.getFov` call sites in `GameRenderer`'s bytecode). The real per-frame value comes from `Camera.update(DeltaTracker)` (itself called from `GameRenderer.update(DeltaTracker)`, on the genuine render path — confirmed via `javap -c -p` on `GameRenderer`), which calls the private `calculateFov(float)`/`calculateHudFov(float)` and PUTFIELDs the results directly into `this.fov`/`this.hudFov`, then immediately feeds `this.fov` into `setupPerspective(...)`. `tickFov()` (called from `Camera.tick()`, per-tick not per-frame) only updates a smoothing field `fovModifier`/`oldFovModifier` consumed by `calculateFov`, and never itself assigns `this.fov`/`this.hudFov`. | `net.minecraft.client.render.GameRenderer.getFov(Camera, float, boolean)` — private, takes the `Camera` as a parameter; `net.minecraft.client.render.Camera` itself has zero FOV-related members. Confirmed (via `javap -c`) to be the single choke point already reached by all of vanilla's own FOV-consuming call sites inside `GameRenderer` (`getBasicProjectionMatrix`, `getProjectionMatrix`, `updateCameraState`, plus one more internal caller) | Correct choke point is the private `Camera.calculateFov(float)` method (returns the value about to be assigned to `this.fov`, which alone feeds the real world-render projection via `setupPerspective`) — **not** the public `Camera.getFov()` accessor, which is dead code for rendering purposes on 26.1/26.2 (only reached from `projectHorizonToScreen()`). Identical bytecode shape confirmed on both 26.1 and 26.2 via `javap -c -p` (no divergence between the two 26.x builds for this one). | `tweaks` (Zoom FOV wiring spec, `docs/specs/tweaks-zoom-fov.md`, mandatory `javap` pass before mixin design; correction in `docs/specs/tweaks-zoom-fov-fix.md`) |
| Detached-camera movement-key routing (needed for Freecam, T14) — **real, load-bearing correction to the spec/plan's own "single highest-risk item, fully unconfirmed" framing: vanilla already solves this for free, confirmed via `javap -c` bytecode trace against all three platforms' resolved merged jars, no dedicated mixin needed at all.** `ClientPlayerEntity.isCamera()`/`LocalPlayer.isControlledCamera()` (`return client.getCameraEntity() == this`) already gates every *position-affecting* consumer of the local player's raw per-tick key-input object: the walking-movement method (`tickMovementInput`/`applyInput`) and the creative-flight vertical-velocity block inside `tickMovement` (jump/sneak while flying) both check it before reading `this.input`. Once `MinecraftClient.setCameraEntity`/`Minecraft.setCameraEntity` points away from the real player, this gate goes false and the real player simply stops moving from WASD/jump/sneak — this is the same mechanism vanilla already uses for spectator-follow-another-entity. **Residual, out-of-scope-for-v1 quirk (confirmed, not fixed):** a few purely cosmetic/network-informational consumers of the same raw input object are NOT gated by this check on any platform (e.g. `isSneaking()`, and the per-tick informational `PlayerInputC2SPacket` vanilla already sends whenever raw input changes, unrelated to actual movement authority) — the real player may still visually crouch/report sneak-pressed while Freecam reuses the Sneak key to fly down. This does not move the player or send a position packet, so it does not violate the hard "player doesn't move" requirement. | `net.minecraft.client.network.ClientPlayerEntity.isCamera()` (`protected boolean`); raw per-tick input object `ClientPlayerEntity.input` (`net.minecraft.client.input.Input`, real impl `KeyboardInput`), record `net.minecraft.util.PlayerInput` (`forward/backward/left/right/jump/sneak/sprint`); movement-affecting consumers: `tickMovementInput()`, the `PlayerAbilities.flying`-gated vertical block inside `tickMovement()` | `net.minecraft.client.player.LocalPlayer.isControlledCamera()` (`protected boolean`, identical shape); raw per-tick input object `LocalPlayer.input` (`net.minecraft.client.player.ClientInput`, real impl `KeyboardInput`), record `net.minecraft.world.entity.player.Input` (**`shift()`, not `sneak()`** — one genuine field-name divergence from the Yarn side); movement-affecting consumers: `applyInput()`, same vertical-flight block inside `tickMovement()`. Confirmed identical on 26.1 and 26.2. | `tweaks-no-rain-freecam` (T14 Freecam implementation, mandatory-first-step `javap` pass) |
| Detached-camera "hide/show my own body" render check (needed for Freecam's `showOwnBody`, T14) — **the spec's own Architecture assumption was backwards, corrected via `javap -c` bytecode trace: `showOwnBody = false` is what already happens for free once the camera detaches, and `showOwnBody = true` is the direction that needs a mixin, not the reverse.** The real check (`entity instanceof ClientPlayerEntity/LocalPlayer && camera's focused entity != entity` → skip rendering) is the SAME code path vanilla already uses to hide a spectating player's own invisible body while following another entity — it unconditionally hides the real player's body whenever the camera's focus isn't the real player, regardless of `showOwnBody`. Implemented as a `@Redirect` (not `@Inject`) on the 4th (0-indexed 3rd, confirmed stable via `javap -c` ordinal count) of the containing method's several same-target calls, spoofing the return value to the real player reference when Freecam is active and `showOwnBody == true` — no local-variable capture needed, since there is only ever one `ClientPlayerEntity`/`LocalPlayer` instance per client, so the redirect can return that fixed reference rather than needing to know which entity the enclosing loop is currently on. | Check lives in `net.minecraft.client.render.WorldRenderer.fillEntityRenderStates(Camera, Frustum, RenderTickCounter, WorldRenderState)`, keyed off `Camera.getFocusedEntity()`/`Camera.isThirdPerson()`, `instanceof ClientPlayerEntity` | **26.1**: same check, renamed class/method: `net.minecraft.client.renderer.LevelRenderer.extractVisibleEntities(Camera, Frustum, DeltaTracker, LevelRenderState)`, keyed off `Camera.entity()` (not `getFocusedEntity()`)/`Camera.isDetached()` (not `isThirdPerson()`), `instanceof LocalPlayer`. **26.2 diverges from 26.1, not just a rename**: `LevelRenderer` has no `extractVisibleEntities` method at all — the entire entity-visibility-extraction responsibility moved to a new dedicated class, `net.minecraft.client.renderer.extract.LevelExtractor`, same method name/signature/ordinal pattern otherwise unchanged. A Freecam-style mixin needs a **different target class on 26.2 than on 26.1** despite both being "26.x Mojmap." | `tweaks-no-rain-freecam` (T14 Freecam implementation, `javap` pass) |
| Left/right-click interaction suppression entry points (needed for Freecam, T14 — this exact target was never analyzed by the spec at all, flagged by the plan as a genuinely new risk item) — confirmed via `javap` against each platform's own resolved merged Minecraft jar, matching the plan's own candidate guess exactly | `net.minecraft.client.MinecraftClient.doAttack()` (`private boolean`, left-click/attack) and `.doItemUse()` (`private void`, right-click/use), both called each tick from `handleInputEvents()` | `net.minecraft.client.Minecraft.startAttack()` (`private boolean`) and `.startUseItem()` (`private void`) — confirmed identical on 26.1 and 26.2 | `tweaks-no-rain-freecam` (T14 Freecam implementation, `javap` pass) |
| Block-collision-sweep primitive for a custom, never-world-added `Entity` subclass to reuse for its own movement (needed for Freecam's `noclip = false`, T14) — **better than the spec's own draft assumed: confirmed `public static`, not merely `protected`, via `javap`, so no subclass privilege is even required for this specific part** (a plain static-method call from any class works); a subclass is still used regardless, since `MinecraftClient.setCameraEntity`/`Minecraft.setCameraEntity` requires a real `Entity` anyway. Also used: `Entity`'s `protected static` movement-input-to-yaw-relative-velocity helper (needs a same-hierarchy passthrough since the caller, `FreecamTicker`, is not itself an `Entity` subclass), and the world-facing collision-shape lookup helper. `Entity.noClip`/`noPhysics` (block-collision-only toggle) is itself renamed across the mapping boundary. | `Entity.adjustMovementForCollisions(Entity, Vec3d, Box, World, List<VoxelShape>)`, `Entity.findCollisions(Entity, World, Box)` (both `public static`); `Entity.movementInputToVelocity(Vec3d, float, float)` (`protected static`); block-collision-only toggle field `Entity.noClip` | `Entity.collideBoundingBox(Entity, Vec3, AABB, Level, List<VoxelShape>)`, `Entity.collectAllColliders(Entity, Level, AABB)` (both `public static`, confirmed identical 26.1/26.2 — 26.2 additionally has an overloaded `collideBoundingBox(CollisionContext, ...)` not present on 26.1, unused by this feature); `Entity.getInputVector(Vec3, float, float)` (`protected static`); block-collision-only toggle field **`Entity.noPhysics`, not `noClip`** (confirmed identical 26.1/26.2) | `tweaks-no-rain-freecam` (T14 Freecam implementation, `FreecamCameraEntity`, `javap` pass) |
| Built-in `EntityType<?>` constants (e.g. the zero-collision `MARKER` type, reused as a placeholder for a custom never-spawned `Entity` subclass that needs *a* type but never cares which) — **real 26.1-vs-26.2 divergence, not just the expected Yarn-vs-Mojmap rename, confirmed via `javap`**: the plan's own "26.1/26.2 usually match" assumption fails here too, same category of surprise as the weather-rendering and own-body-hide-check rows above | `net.minecraft.entity.EntityType.MARKER` — declared directly as a `public static final` field on `EntityType` itself, standard pre-26.x shape | **26.1**: `net.minecraft.world.entity.EntityType.MARKER` — same shape, declared directly on `EntityType` itself. **26.2**: `EntityType` declares **no built-in constant fields at all** (confirmed via `javap -p`, only `CODEC`/`STREAM_CODEC` remain) — every built-in type constant, including `MARKER`, moved to a new dedicated holder class, `net.minecraft.world.entity.EntityTypes` (plural) — `EntityTypes.MARKER`, not `EntityType.MARKER`. | `tweaks-no-rain-freecam` (T14 Freecam implementation, `FreecamCameraEntity`, real `:platform:fabric-26.2:compileJava` failure after code that compiled fine on 26.1) |
| Rain/snow precipitation render + ambient-sound choke point (needed for No Rain, T13) — **confirmed via `javap -p -s -c` against all three platforms' resolved merged jars; renderer class renames as expected across the obfuscation boundary, but 26.1 and 26.2 additionally diverge from EACH OTHER for the sound/splash-particle half, not just a rename.** All three sides split the same two responsibilities into a build/render pair (data-build populates a `WeatherRenderState`'s two public mutable `List<Piece/ColumnInstance>` fields, `rainPieces`/`snowPieces` (1.21.11) or `rainColumns`/`snowColumns` (26.x); a separate draw method consumes them) plus a third method that spawns rain-splash particles and plays/updates the continuous `SoundEvents.WEATHER_RAIN`/`WEATHER_RAIN_ABOVE` ambient loop, gated per-column on `getPrecipitationAt(pos) == RAIN` (confirmed via bytecode trace on all three — snow never triggers this sound/particle path on any platform, so suppressing it never needs an `includeSnow` split). The private `getPrecipitationAt` accessor does NOT need an `@Invoker`/duplicated-lookup workaround for the visual `includeSnow` split (a materially simpler alternative than either option the `tweaks-no-rain-freecam.md` spec proposed) — the build method's `TAIL` injection (confirmed via `javap -c` to be the method's true final `return`, distinct from an earlier "rain gradient is zero" early-return) can just `.clear()` the public `rainPieces`/`rainColumns` list (and additionally `snowPieces`/`snowColumns` when suppressing snow too) after vanilla has already split rain from snow into those two lists itself. | `net.minecraft.client.render.WeatherRendering` (owned by `WorldRenderer`, confirmed present exactly as the spec's medium-confidence guess predicted for this literal `1.21.11+build.6` pin): `buildPrecipitationPieces(World, int, float, Vec3d, WeatherRenderState)`, `renderPrecipitation(VertexConsumerProvider, Vec3d, WeatherRenderState)`, `addParticlesAndSound(ClientWorld, Camera, int, ParticlesMode, int)` (one real signature deviation from the spec's own 4-arg guess: a trailing extra `int`) | **26.1**: `net.minecraft.client.renderer.WeatherEffectRenderer` — `extractRenderState(Level, int, float, Vec3, WeatherRenderState)`, `render(Vec3, WeatherRenderState)`, `tickRainParticles(ClientLevel, Camera, int, ParticleStatus, int)` (same 3-method shape as 1.21.11, just Mojmap-renamed, matching the spec's own named `WeatherEffectRenderer` fallback guess exactly). **26.2**: same class exists with `extractRenderState`/`render` (note: `extractRenderState`'s signature further narrows to `(ClientLevel, float, Vec3, WeatherRenderState)` — no `int` radius param, `Level`→`ClientLevel` — a real 26.1-vs-26.2 signature divergence, not just identical Mojmap churn) but **`tickRainParticles` does not exist on `WeatherEffectRenderer` at all** — that responsibility moved onto a new no-arg `net.minecraft.client.multiplayer.ClientLevel.tickWeatherEffects()` method instead (confirmed via `javap -c` bytecode trace: identical rain-gradient early-return, `getPrecipitationAt`-gated loop, and `WEATHER_RAIN`/`WEATHER_RAIN_ABOVE` playback shape, just relocated off the renderer and onto the level). A No-Rain-style tweak's sound-suppression mixin must target a **different class per 26.x sub-version** (`WeatherEffectRenderer` on 26.1, `ClientLevel` on 26.2) even though both are otherwise "26.x Mojmap." | `tweaks-no-rain-freecam` (T13 No Rain implementation, mandatory-first-step `javap` pass across all three resolved jars) |

## Multi-Version Porting Runbook

### The Obfuscation Boundary
Minecraft/Fabric versions 1.21.11 and older are obfuscated. 26.1 and newer are not.
This single fact drives most of what differs between platform modules — check
which side of the boundary a version falls on before doing anything else.

| | ≤1.21.11 (obfuscated) | ≥26.1 (unobfuscated) |
|---|---|---|
| Loom plugin id | `net.fabricmc.fabric-loom-remap` | `net.fabricmc.fabric-loom` |
| Mappings | Yarn, needs a `mappings` dependency | Mojang, no `mappings` dependency |
| Mod dependency configs | `modImplementation` / `modApi` / `modCompileOnly` | plain `implementation` / `api` / `compileOnly` |
| Java version | 21 | 25 |
| Class naming example | `net.minecraft.util.Identifier`, `Identifier.of(ns, path)` | `net.minecraft.resources.Identifier`, `Identifier.fromNamespaceAndPath(ns, path)` |
| Final jar task | `remapJar` (via `assemble`) | `jar` (via `assemble`) |

Yarn stops being maintained after 1.21.11 — there will be no obfuscated version
newer than that to port to.

### Looking Up Version Coordinates
Do not trust WebFetch/WebSearch summaries for exact version strings — the
summarizer has confused real historical Fabric Loom version numbers with
fictional ones when asked about versions past its training data. Always
confirm coordinates with raw HTTP fetches (`curl` via Bash), which return
unsummarized JSON/XML:

- Game versions: `curl -s https://meta.fabricmc.net/v2/versions/game`
- Loader versions: `curl -s https://meta.fabricmc.net/v2/versions/loader`
- Yarn mappings for a game version: `curl -s https://meta.fabricmc.net/v2/versions/yarn/<version>`
- Loom plugin versions: `curl -s https://maven.fabricmc.net/net/fabricmc/fabric-loom/net.fabricmc.fabric-loom.gradle.plugin/maven-metadata.xml`
  (swap `fabric-loom` for `fabric-loom-remap` for obfuscated versions)
- Fabric API versions for a game version: grep the game version suffix out of
  `curl -s https://maven.fabricmc.net/net/fabricmc/fabric-api/fabric-api/maven-metadata.xml`

Sanity-check freshness via the `lastUpdated` field in the maven-metadata XML —
it should be recent, not stale.

### Adding a New `platform/fabric-<version>` Module
1. Duplicate an existing platform module on the same side of the obfuscation
   boundary as a starting point (same plugin id, same dependency style).
2. Give the new module its own `gradle.properties` overriding
   `minecraft_version`, `fabric_api_version`, and (if obfuscated) `yarn_mappings`
   and `loom_version` — do not touch the root `gradle.properties`, which stays
   pinned to the primary/newest supported version.
3. Add the module to `settings.gradle`.
4. If the new version crosses the obfuscation boundary relative to existing
   modules, check the shared modules' Java level (see below) still covers it.
5. Build it directly (`:platform:fabric-<version>:build`) and let real compile
   errors — not guesses — drive any API renames. Class/method names across the
   Yarn/Mojang mapping boundary cannot be reliably predicted from search
   results alone.
6. Regenerate the run configuration
   (`:platform:fabric-<version>:configureClientLaunch`) and hand-add a
   corresponding entry to `.vscode/launch.json` — Loom's `vscode` task writes a
   fixed `"Minecraft Client"` name into the single shared launch.json and its
   up-to-date check is broken across subprojects, so it silently no-ops for
   every module after the first. Never rely on it alone in a multi-version
   workspace.

### Java Version Floor
`api`/`common`/`services`/`libraries` are depended on by every platform
module, so they must compile at the *lowest* Java version required by any
supported Minecraft version (currently 21, for 1.21.11), set once in the root
`build.gradle`. Individual platform modules raise their own
`sourceCompatibility`/`targetCompatibility`/`release` upward as their
Minecraft version requires (25 for 26.1/26.2). Never raise the shared floor to
match the newest platform module — that breaks the build for every older one.

### Toolchain Note
`javac` can target an older `--release` than the JDK running it, but never a
newer one. Gradle's automatic toolchain provisioning was unreliable at
locating an already-installed JDK on this machine (Windows, JDK found under
`Program Files\Microsoft\...` was not auto-detected). The working setup is:
run the whole Gradle invocation under the newest JDK any supported version
needs (currently 25), and let older platform modules cross-compile down via
`options.release`. Don't reach for `java.toolchain {}` per-subproject to solve
this unless auto-provisioning (a configured download repository) is set up —
it will fail closed instead of falling back.
